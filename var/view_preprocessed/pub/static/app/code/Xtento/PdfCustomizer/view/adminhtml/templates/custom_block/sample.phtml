<?php $objectManager = \Magento\Framework\App\ObjectManager::getInstance(); $orderId = $this->getOrder(); $order = $objectManager->get('\Magento\Sales\Model\OrderFactory')->create()->load($orderId); /*$objectManager = \Magento\Framework\App\ObjectManager::getInstance(); $orderItemId = $this->getItem(); $item = $objectManager->create('Magento\Sales\Model\Order\ItemFactory')->create()->load($orderItemId); if ($item->getParentItem()) { $parentId = $item->getParentItem()->getProductId(); $product = $objectManager->create('Magento\Catalog\Model\Product')->load($parentId); if ($product->getId() && $product->getData('some_attribute') == 'test') { echo "Hello!"; } }*/ ?> This is a test. Order Number: <?php echo $order->getIncrementId(); ?><br/><br/> Use this as a sample to write custom PHP logic and embed it in your PDF templates.